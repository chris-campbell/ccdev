import React from "react";
import * as S from "./styles/styles";

const Description = ({ description }) => {
  return <S.DescriptionContainer>{description}</S.DescriptionContainer>;
};

export default Description;
